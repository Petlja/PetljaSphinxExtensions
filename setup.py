from setuptools import setup, find_packages

setup(
    name='petlja_sphinx_extensions',
    version='0.1',
    description='PetljaSphinxExtensions',
    author='Petlja',
    packages=find_packages(),
    install_requires=[
        'sphinx',
    ],
)